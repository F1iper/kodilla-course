package com.kodilla.good.patterns.challenges.food2door;

public class RegularClient implements ClientDatabase {

    @Override
    public boolean isRegular(Producer producer){

        // todo : if producer regular -> .......... if not -> ............
        return true;
    }
}